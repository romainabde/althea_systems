# =============================================================================
# Copier ce fichier vers .env et renseigner DATABASE_URL (même URI que auth-cart-service).
# =============================================================================

PORT=8081

# PostgreSQL Neon (même base que admin-service, catalog-service, auth-cart-service)
# Exemple de forme (à adapter) :
# DATABASE_URL="postgresql://USER:MOTDEPASSE@ep-xxxx.region.aws.neon.tech:5432/neondb?sslmode=require"
DATABASE_URL=
