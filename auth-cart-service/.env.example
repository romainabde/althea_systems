# =============================================================================
# Copier ce fichier vers .env et compléter les valeurs.
# =============================================================================

PORT=8080
JWT_SECRET=change-moi-en-secret-long-et-aleatoire-pour-le-dev
FRONTEND_URL=http://localhost:3000
STRIPE_KEY=sk_test_remplace_par_ta_cle_stripe_test

# PostgreSQL Neon (même projet / même base SQL que admin-service & catalog-service)
# Dashboard Neon > Connexion > copier la connection string URI (avec SSL).
# Exemple de forme (à adapter) :
# DATABASE_URL="postgresql://USER:MOTDEPASSE@ep-xxxx.eu-west-2.aws.neon.tech/neondb?sslmode=require"
DATABASE_URL=

EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASS=
